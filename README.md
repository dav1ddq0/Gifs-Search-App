# Gifs-Search-App

A web application to search and view GIFs using the Giphy API (or any GIF API you choose).

## 📌 Description

Gifs-Search-App allows you to:

- Search GIFs by keyword.  
- View results in a grid layout.  
- Click a GIF to see it larger (optional, depending on UI).  
- Lightweight frontend app built with React.

## 🚀 Features

- Simple and intuitive interface  
- Instant GIF search  
- State management for results and loading  
- Responsive design (works on all screen sizes)  
- Uses Giphy API (or configured GIF API)  

## 🧰 Technologies

- **React** (Create React App)  
- **TypeScript**  
- **CSS / SCSS** for styling  
- **Fetch / Axios** for API calls  

## 📁 Project Structure

| Folder | Description |
|---|---|
| `src/` | Application source code |
| `public/` | Static public files |
| `src/components/` | Reusable React components |
| `src/services/` | API service logic |
| `src/styles/` | CSS / SCSS files |
| `src/App.tsx` | Main app component |
| `src/index.tsx` | App entry point |

## 🔧 Installation

1. Clone the repository:

   ```bash
   git clone https://github.com/dav1ddq0/Gifs-Search-App.git
